apply plugin: 'java'
apply plugin:'application'

mainClassName = "Main"

sourceCompatibility = 1.7
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile files('./jnibwapi.jar')

    testCompile group: 'junit', name: 'junit', version: '4.11'
}

run {
}